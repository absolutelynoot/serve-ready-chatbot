# This is an example .env file. It should not be used as is, but rather as a template for creating your own .env file.

# Database configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mydatabase
DB_USER=myusername
DB_PASSWORD=mypassword

# API keys
API_KEY_1=yourapikeyhere
API_KEY_2=anotherapikeyhere
